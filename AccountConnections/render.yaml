services:
  - type: web
    name: linkedin-scraper
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn linkedin_scraper_api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.5